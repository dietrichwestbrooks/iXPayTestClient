namespace Wayne.Payment.Tools.iXPayTestClient.Infrastructure.Events
{
    public enum ConnectionEventType
    {
        Failed,
        Disconnected,
        Connecting,
        Connected,
        Pulse,
        Disconnecting
    }
}
